/*import 'package:flutter/material.dart';

class RoundedButton extends StatelessWidget {

  final String btnName;
  final Icon? icon;
  final Color? bgColor;
  final TextStyle? textStyle;
  final voidCallback? callback; // voidCallback 1 aisa function hota hai , na kuch leta hai na kuch deta hai wo baas jo function me code likha hota hai wo baas exicute kar deta hai


  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: (){
      callback!();

    }, child: icon!=null ? Row(






    ));

  }

  const RoundedButton({super.key, required this.btnName,
    this.icon,
    this.bgColor = Colors.purple,
    this.textStyle,
    this.callback
  });


}*/
